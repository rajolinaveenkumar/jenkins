pipeline {
    agent any

    stages{

        stage('Build') {
            steps {
                script {
                    sh """
                        echo "Hello this is build"
                    """
                }
            }
        }
    }
}